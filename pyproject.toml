[project]
name = "multi-model-tester"
version = "0.1.0"
description = "A simple multi-model tester UI with test case management."
requires-python = ">=3.8"

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-q"
testpaths = ["tests"]
