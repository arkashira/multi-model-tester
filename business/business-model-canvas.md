Generated `business-model-canvas.md` for **multi-model-tester** (overwrote a stale agent-orchestra canvas that was in the path).

Key opinionated calls baked in:

- **Beachhead = startup AI engineers already multi-homed** across 2–4 providers — they have the pain *today* and the budget, vs. solo hackers (funnel only) or enterprise (slow).
- **Open-core SaaS** as the primary engine: Free OSS → Pro $29 → Team $99 → Business $499, with Enterprise self-host at $8K–$25K/yr. OSS is non-negotiable for a dev-eval tool's trust + distribution.
- **The wedge metric is cost arbitrage** — "40–70% token-cost cut" is the hard-number hook that converts; I made it the lead value prop, not a feature.
- **Moat = the adapter/pricing-maintenance treadmill** — keeping pace with provider model/price churn is the recurring effort competitors underestimate; I called it out as both a Key Resource and the main cost-structure risk.
- Leveraged the company's ~9.4M auto / ~6.4M instr-resp datasets as a Key Resource for pre-built edge-case suites — a real differentiator vs. an empty-harness competitor.

THB at 1 USD ≈ 36.5 (2026 Q2). Ready for the next section (suggest `pricing.md` or `gtm.md` to pressure-test the revenue assumptions above).